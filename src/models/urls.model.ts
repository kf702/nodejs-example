import { ShortUrl } from '@/interfaces/urls.interface';

// for test, use array instead mysql

const ShortUrlModel: ShortUrl[] = [
    { id: 1, short: '12345678', long: 'http://abc.com/abc.html' },
];

export default ShortUrlModel;
